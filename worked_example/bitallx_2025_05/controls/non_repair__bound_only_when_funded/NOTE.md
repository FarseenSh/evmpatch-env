# non_repair__bound_only_when_funded

**kind:** non_repair  
**expected:** not_solved 0.0, oracle Success (PoC catches it)

The reference bound applied only when totalSendAmount > 0. The whole incident is totalSendAmount = 0, so the shipped PoC catches it with no help from the security suite.

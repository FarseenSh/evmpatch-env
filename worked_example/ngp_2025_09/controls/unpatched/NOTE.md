# unpatched

**kind:** baseline

**expected:** not_solved / oracle Success / canary empty_patch

Shipped vulnerable source, no patch. The exploit must still reproduce, every legitimate-use hidden test must pass, and every declared security obligation must FAIL (that is what makes them able to separate a repair from a non-repair).

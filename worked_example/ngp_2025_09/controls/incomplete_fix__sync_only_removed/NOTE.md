# incomplete_fix__sync_only_removed

**kind:** incomplete_repair

**expected:** not_solved 0.0 / poc_blocked / canary residual_vulnerability

The load-bearing incomplete repair: only the mid-transfer sync() is deleted, so the PoC is blocked and an exploit oracle alone would grade it like the reference, while the pair is still drained inside a seller's own transfer.

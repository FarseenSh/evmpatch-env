[profile.default]
src = 'src'
test = 'test'
out = 'out'
libs = ['lib']
evm_version = 'shanghai'
optimizer = true
optimizer_runs = 200
fs_permissions = [{ access = "read", path = "./" }]

[rpc_endpoints]
bsc = "${TASK_RPC_URL}"
